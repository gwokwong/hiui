export enum ButtonType {
  Primary = 'primary',
  Default = 'primary',
  Warning = 'warning',
  Text = 'text',
  Link = 'link',
}

export const ButtonTypeMap = {
  [ButtonType.Primary]: 'is-primary',
}
